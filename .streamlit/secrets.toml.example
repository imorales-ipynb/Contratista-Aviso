# Copiar como .streamlit/secrets.toml y completar con valores reales.
# En Streamlit Cloud: Settings → Secrets (pegar el contenido directamente).

# Activa el modo solo-parquet (sin conexión a SQL Server)
CLOUD_MODE = "true"
